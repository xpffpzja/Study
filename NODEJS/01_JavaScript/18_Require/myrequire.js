// node myrequire.js

const checkNumber = require('./func02');

const result = checkNumber(15);
console.log(`15 는 ${result}`);


// 객체 형태로 공유된 자료는 객체형태나 객체를 저장할 변수로 받으면 되고
// 위와 같이 자료만 공유된 형태는 변수만 사용하면 받을 수 있음.










// node myrequire.js